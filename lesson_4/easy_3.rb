# The class method is called on the object itself (self), and returns
# the class that the object is an instance of - either Car or Truck.
# A string representation of that class is then output via string interpolation.